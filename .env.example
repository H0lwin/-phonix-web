# ============================================================================
# PHONIX PRODUCTION ENVIRONMENT CONFIGURATION
# ============================================================================
# این فایل یک مثال از متغیرهای محیطی است
# قبل از deployment، آن را به .env کپی کنید و مقادیر را تغییر دهید
# ============================================================================

# ============================================================================
# 🔐 DJANGO CORE SETTINGS
# ============================================================================

# Development یا Production
# در پروداکشن باید False باشد
DEBUG=False

# کلید محرمانه Django (برای production کلید منحصر به فرد و قوی استفاده کنید)
# می‌توانید از این دستور یک کلید تولید کنید:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-development-key-change-in-production

# Hosts مجاز (کاما‌جدا شده)
# مثال برای production: yourdomain.com,www.yourdomain.com
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================================================
# 📊 DATABASE CONFIGURATION
# ============================================================================

# MySQL (توصیه برای production)
DATABASE_ENGINE=django.db.backends.mysql
DATABASE_NAME=Phonix_suite
DATABASE_USER=phonix_user
DATABASE_PASSWORD=your_strong_password_here
DATABASE_HOST=localhost
DATABASE_PORT=3306

# یا SQLite (برای توسعه و تست)
# DATABASE_ENGINE=django.db.backends.sqlite3
# DATABASE_NAME=db.sqlite3

# یا PostgreSQL (گزینه دیگر)
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=Phonix_suite
# DATABASE_USER=phonix_user
# DATABASE_PASSWORD=your_strong_password_here
# DATABASE_HOST=localhost
# DATABASE_PORT=5432

# ============================================================================
# 🔐 SECURITY SETTINGS
# ============================================================================

# HTTPS enforcement (production)
SECURE_SSL_REDIRECT=True

# HTTPS only cookies
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True

# HSTS (HTTP Strict Transport Security)
SECURE_HSTS_SECONDS=31536000
SECURE_HSTS_INCLUDE_SUBDOMAINS=True
SECURE_HSTS_PRELOAD=True

# ============================================================================
# 👤 ADMIN USER (برای ایجاد اولیه)
# ============================================================================

# این مقادیر توسط init_admin.py استفاده نمی‌شوند (تعاملی است)
# اما می‌توانید برای مرجع نگه‌دارید

# ADMIN_USERNAME=admin
# ADMIN_EMAIL=admin@phonix.ir
# ADMIN_PASSWORD=strong_password_here

# ============================================================================
# 🌐 SERVER CONFIGURATION
# ============================================================================

# محدوده سرور (معمولاً فقط localhost در production)
SERVER_HOST=127.0.0.1

# پورت برای Gunicorn
SERVER_PORT=8000

# ============================================================================
# 📧 EMAIL CONFIGURATION (اختیاری)
# ============================================================================

# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your_email@gmail.com
# EMAIL_HOST_PASSWORD=your_app_password

# ============================================================================
# 🗂️  FILE UPLOAD CONFIGURATION
# ============================================================================

# حداکثر سایز فایل آپلود (بایت)
# DATA_UPLOAD_MAX_MEMORY_SIZE=10485760

# ============================================================================
# 🔍 LOGGING CONFIGURATION
# ============================================================================

# سطح لاگ
# LOG_LEVEL=INFO

# ============================================================================
# NOTES FOR PRODUCTION
# ============================================================================
# 
# 1. SECRET_KEY: باید یک کلید قوی و تصادفی باشد
#    می‌توانید از این دستور تولید کنید:
#    python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
#
# 2. DATABASE_PASSWORD: از رمز عبور قوی استفاده کنید
#    الزامات:
#    - حداقل 12 کاراکتر
#    - حروف بزرگ و کوچک
#    - اعداد و نمادهای خاص
#
# 3. ALLOWED_HOSTS: تنها دامنه‌های واقعی اضافه کنید
#    مثال: example.com,www.example.com
#
# 4. DATABASE: MySQL یا PostgreSQL برای production توصیه می‌شود
#
# 5. SSL Certificate: از Let's Encrypt یا سرویس دیگری استفاده کنید
#
# 6. Backups: روزانه database و media files را بکاپ کنید
#
# 7. Monitoring: سیستم‌ی برای مانیتورینگ سرور و اپلیکیشن داشته باشید
#
# برای جزئیات بیشتر مراجعه کنید: PRODUCTION_DEPLOYMENT.md

